# tempertemper.net


## Development environment

### Set up
- Run `npm install` to install the node packages

### Running
- `cd` to project root
- Run `npm start`

### Updating dependencies
- Run `npm update`
